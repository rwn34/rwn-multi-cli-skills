## 2026-07-21 19:42 (UTC+7) — kimi-cli
- Action: After retiring the v7 chain handoff, inspected executor worktrees and found the canonical `.ai/` deletion bug active in all three: `claude`, `kiro`, and `opencode` worktrees showed every `.ai/**` file as deleted (`D` in git status); `kiro` and `opencode` had no `.ai/` directory at all, while `claude` had empty `.ai/activity/` and `.ai/handoffs/` skeletons. Removed all three broken worktrees with `bash scripts/wt-bootstrap.sh --remove /c/Users/rwn34/Code/rwn-multi-cli-skills claude kiro opencode` to prevent any accidental commit of mass deletions. Primary checkout `.ai/` remains intact.
- Files: `.ai/activity/entries/20260721T114200Z-kimi-cli-worktree-deletion-incident-*.md`
- Decisions: Treat this as a live recurrence of the canonical-deletion bug already under investigation in `.ai/handoffs/to-kiro/open/202607211105-diagnose-canonical-ai-deletion.md`. The worktree removal destroys some forensic state, but Kiro's completed handoff already captured the symptom in its report. Fresh worktrees will be created by wt-bootstrap on next dispatch. The root cause still needs Kiro's diagnosis.
