## 2026-07-21 18:25 (UTC+7) — kiro
- Action: Processed handoff 20260721T111700Z-kiro-contract-post-freeze-cleanup — updated Kiro-native activity-log wording to the ADR-0010 entry-spool model (protocol v3 -> v4, "prepend" -> "write an entry file"). `log.md` is still on disk (pre-archive) but the primary repo's pre-commit hook already rejects new writes to it (Wave-3), so this entry uses the spool per current enforcement rather than the transitional prepend text the handoff itself pointed at.
- Files: .kiro/steering/00-ai-contract.md (lines ~10, ~76, ~94: "entries you prepend here" -> "entry files you write here"; "protocol v3" -> "protocol v4"; "prepend an activity-log entry" -> "write an activity-log entry file"); .kiro/hooks/guards.json (line ~74: "prepend an activity-log entry" -> "write an activity-log entry file")
- Decisions: bash .kiro/hooks/test_hooks.sh -> PASS 70/70; bash .ai/tools/lint-handoff.sh -> OK. bash .ai/tools/sync-replicas.sh --check FAILED (exit 1, "Checked: 0 replicas, Drift: 1") for an unrelated reason: it refuses to regenerate because git ls-files -v can't verify skip-worktree state of .ai/instructions/karpathy-guidelines/principles.md in this worktree (pre-existing junction issue, unrelated to the two replicas the handoff flagged: self-grep-verify.md/operating-prompt.md). Not blocking on it — out of this handoff's scope. Also corrected an initial mistake: first wrote this entry as a log.md prepend per this handoff's own (now-stale) wording, then discovered the primary repo's core.hooksPath pre-commit hook unconditionally rejects staged log.md writes (ADR-0010 Wave-3, landed on exec/kimi/20260721-framework-finalization@4674511) — reverted the log.md edit and re-logged here instead.
