## 2026-07-21 23:35 (UTC+7) — claude-cockpit
- Action: Found and documented a second occurrence of the canonical `.ai/` deletion bug, this one PARTIAL (3 tracked files, not 438), on branch `exec/claude/202607211616-delegate-adr0010-freeze-to-claude`. `git log --oneline -2 -- <path>` on each deleted path shows only add/modify commits and no retirement; `to-kiro/open/202607211616-delegate-canonical-ai-deletion-to-kiro.md` was created by the immediately preceding commit `f3d37e9` and was already gone from the working tree. Restored all three via `git restore` (byte-identical to HEAD, no content authored). Committed the freeze-blocked artifacts as `bb08bd2` staging four explicit paths — no `git add -A`, `.ai/.snapshot-manifest` left untracked. Appended a "Recurrence #2" section to the Kiro diagnosis handoff and an addendum to the kimi-cockpit return handoff.
- Files: .ai/handoffs/to-kiro/open/202607211105-diagnose-canonical-ai-deletion.md, .ai/handoffs/to-kimi-cockpit/open/202607211618-claude-adr0010-freeze-return.md, .ai/activity/entries/20260721T163500Z-claude-canonical-deletion-recurrence-2-e4d8.md
- Decisions: Deliberately avoided `git add -A` — that is precisely the command that would have committed the wipe as intent, which is the failure mode the diagnosis handoff names. New hypothesis handed to Kiro: a partial deletion set points at stale-snapshot sync-back removing canonical files created AFTER the snapshot was taken (all three deleted files were recent), which would make the 438-file wipe the degenerate case of the same mechanism rather than a separate bug — and a 3-file reproduction is far cheaper to instrument than a full wipe.
