## 2026-07-22 02:05 (UTC+7) — claude-cockpit
- Action: Cut v0.0.53 (owner-approved Tier C: "0.0.53 i mean") via release-engineer → `914298f`. Promoted all 10 `## [Unreleased]` bullets into `## [0.0.53] - 2026-07-22` as a pure heading insertion (zero bullet lines in the diff, so ADR-0012 provenance is satisfied byte-identically). Bumped `package.json` + both `package-lock.json` version fields. `gates` 29874821161 SUCCESS and `release` 29874821175 SUCCESS on `914298f`; release v0.0.53 published with 4 assets (matches `EXPECTED_ASSET_COUNT: 4`), tag auto-cut by the workflow. **main is no longer red** — the ADR-0012 version-bump detective is cleared for the first time since `95b0ebe`. Then wrote the next kimi-cockpit handoff.
- Files: tools/multi-cli-install/package.json, tools/multi-cli-install/package-lock.json, CHANGELOG.md, .ai/handoffs/to-kimi-cockpit/open/20260722T020000Z-post-0053-state-and-remaining-queue.md
- Decisions: Deliberately left `.ai/.framework-version` (stale at 0.0.45 since before 0.0.46) OUT of the release commit rather than quietly widening its scope — routed to Kimi as R2, since the drift detector in `fleet-health.sh` reads a value nothing currently writes and that is a design question, not a one-liner. **Confirmed six of the seven prior follow-ups are already closed by Kimi** (F1 fail-closed renderer guard `2081666`, F2, F3 SSOT spool update, F5 known-limitations entry, F6.2 issue #133, F7 canonical-deletion root cause via PR #135 `496334a`) — remaining queue is now R1 (handoff retirement copies instead of moves: `326a35c` is 1 file changed / 80 insertions with no deletion, leaving the finalization report live in BOTH `open/` and `done/` — second occurrence of the `b5694d2` class), R2, R3 (freeze now unblocked but stays mine + owner). **Corrected two of my own earlier findings:** the live and installer-asset renderers are now byte-identical (`diff` → IDENTICAL), so my "two divergent implementations" claim does not hold; and my suspicion that Kimi's `sync-ai-state.sh` changes caused the canonical `.ai/` deletion was wrong — they were strictly safer, and the reported deletion was a cosmetic unstaged-` D` artifact of the by-design ADR-0016 snapshot removal. **Two findings escalated to the owner, not actioned:** (1) branch protection on main is decorative for this identity — every push today including the release printed `Bypassed rule violations ... 2 of 2 required status checks are expected` with no `--admin` and no force flag, so checks passed after the fact but were never enforced; (2) `check-version-bump.sh` reports PASS for bump-only commits, and the PR-time `check-changelog-unreleased.sh` that closes that hole has never fired in anger because 0.0.53 went in as a direct push to main. Structural fix is routing bumps through PRs — a process change for the owner, not a unilateral one.
