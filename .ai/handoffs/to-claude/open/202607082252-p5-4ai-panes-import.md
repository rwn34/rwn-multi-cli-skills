# P5 — Import 4ai-panes into tools/4ai-panes/ + queue-badge poller
Status: OPEN
Sender: claude-code
Recipient: claude-code
Created: 2026-07-08 22:52
Auto: no
Risk: B

<!-- Auto: no because step 1 needs the owner present: repo fetch/copy decision
     and possible private-repo access. Everything after is Risk B. -->

<!-- UPDATE 2026-07-08 (owner + reflog inspection): the source repo is LOCAL at
     C:\Users\rwn34\.rwn-auto\rwn-4AI-panes — no GitHub fetch needed.
     Git-state corrections vs. the text below:
     - Branch framework-upgrade-adr0002 tip is 0df6908 (the handoff's 0dcbe73
       was amended twice: ab9efb9 -> 0dcbe73 -> 0df6908). Branch still
       unmerged locally.
     - Local master was `pull --ff-only`d to 06c5d84 on 2026-07-08 — origin
       moved ahead after the branch diverged from 0f365b1. FIRST STEP of the
       import: diff 0df6908's tree against 06c5d84's to determine whether the
       upgrade work was already merged remotely; import whichever tree is the
       true superset, and reconcile (never discard 0df6908 silently).
     - Its embedded framework replicas (.ai/.claude/.kimi/.kiro inside that
       repo) predate today's rebuild — after import, that PROJECT also needs
       the P6 upgrade runbook applied. Do NOT import those framework dirs
       into tools/4ai-panes/ here; import only the launcher itself
       (Launch4Panes.ps1, Selector.ps1, Launch4Panes.vbs, icon.ico, README,
       .4pane-layout handling). -->


## Goal
Fold the Windows 4-pane launcher (https://github.com/rwn34/rwn-4AI-panes) into
this repo at `tools/4ai-panes/` — owner approved 2026-07-08 ("personalised
framework, no need for different repo") — and add a handoff-queue badge to the
selector so the human sees pending Risk-C work without opening files.

## Current state
- Launcher lives in the external repo (PowerShell `Launch4Panes.ps1` +
  `Selector.ps1` + VBS wrapper; `.4pane-layout` persistence).
- This repo knows it only via `.ai/research/4ai-panes-integration-notes.md`.
- Pane launch commands per that note: claude `--dangerously-skip-permissions`,
  kiro-cli `--trust-all-tools`, kimi `--agent-file .kimi/agents/orchestrator.yaml
  --yolo`, crush `--yolo`.

## Target state
1. `tools/4ai-panes/` contains the launcher source (no nested `.git`).
   **Import from the RIGHT state:** the 4AI-panes repo has local branch
   `framework-upgrade-adr0002` @ `0dcbe73` — reviewed + APPROVED by the
   2026-07-07 cloud session but NOT merged to its default branch (see
   done/202607071330 handoff §Current state). Import 0dcbe73's content, not
   the stale default branch.
2. Selector shows per-CLI open-handoff counts (poll `.ai/handoffs/to-*/open/`
   of the selected project) — the "human as low-friction dispatcher" option
   from the integration notes §3.
3. Selector ALSO shows a per-project framework-version badge: read
   `.ai/.framework-version`, display `⚠ outdated / ✔ current / – none`
   (absence = old install). This is layer 1 of the stale-fleet coverage plan
   (done/202607071330 §Agreed coverage plan) — old projects run OLD rules
   (worst case: Crush --yolo with no CRUSH.md), and detection cannot live in
   the old projects' own hooks.
4. Launch commands verified against amended ADR-0002 roles (Kimi orchestrator
   entry point, Kiro default agent set, Crush CRUSH.md present in target).
5. Integration notes updated: repo-import complete; cross-repo coupling
   section rewritten (policy AND launcher now live here).
6. Owner decides the old repo's fate (archive) — Tier C, ask explicitly.

## Steps
1. WITH OWNER: fetch the repo (public → WebFetch/git clone via infra-engineer;
   private → owner copies source in). Strip `.git`.
2. Delegate import + badge feature to infra-engineer (PowerShell edits are
   infra/tooling scope; tools/ is the sanctioned directory).
3. Runtime verification per delivery-integrity: owner launches the selector
   once and confirms panes + badge render — paste/report what was observed.
   No "it should work" claims.
4. doc-writer: update integration notes + tools/4ai-panes/README.md.

## Verification
- (a) `ls tools/4ai-panes/` listing, (b) badge code grep evidence,
  (c) owner-confirmed live launch.

## Next step / future note
After import, P4's polling decision should route "notify the human" through
the selector badge, keeping /loop and schedule for machine dispatch. Breaks
first: Windows Terminal CLI arg changes — pin the `wt.exe` invocation flags in
a comment with the tested WT version.

## Report back with
- (a) file listing, (b) selector screenshot or owner confirmation, (c) notes diff
