# Migrate Kiro to v3 (permissions.yaml + JSON hooks + markdown agents) + re-validate enforcement
Status: PARTIAL — config delivered (additive); live validation BLOCKED. Kept OPEN for claude-code to validate + move.
Sender: claude-code
Recipient: kiro-cli
Created: 2026-07-09 14:30
Auto: yes
Risk: B

## kiro-cli completion note (2026-07-09 14:27)
Executed additively. Full report: `.ai/reports/kiro-cli-2026-07-09-v3-migration.md`.
TWO findings change the ADR-0006 merge decision — read the report before merging.

Delivered (additive; v2 `.json` agents + `.sh` guards untouched):
- `.kiro/agents/orchestrator.md` — v3 Markdown agent with `permissions.rules`
  block (THE repo-committable enforcement path).
- `.kiro/hooks/guards.json` — v3 standalone hooks (`version: v1`, PascalCase
  triggers) reusing the existing pure-bash fail-CLOSED `.sh` guards. No python.
- `.ai/config-snippets/kiro-v3-permissions.yaml` — owner-installed template for
  `~/.kiro/settings/permissions.yaml`.

Deviations from the literal steps (both grounded in the v3 docs, read fresh):
- **Step 1 corrected:** a repo `permissions.yaml` is DEAD TEXT — v3 loads it only
  from `~/.kiro/settings/` or `~/.kiro/workspace-roots/<hash>/` (per-user, outside
  repo); "a cloned repo cannot inject permission rules." Repo enforcement moved to
  the agent-md `permissions` block (delivered).
- **Step 4 NOT RUN (blocked):** (1) orchestrator has no shell; (2) v3 has NO
  headless mode — "classic non-TUI mode does not support the v3 engine, use the
  TUI." So `kiro-cli --v3 chat --no-interactive` cannot run at all. Live probes
  for owner-run TUI validation are in the report §4.
- **Step 5 answer:** there is no working v3 headless invocation today. The
  dispatcher/Selector `--v3` headless pin (commit 52b31fa) is currently
  non-functional; keep headless dispatch on v2 + the ADR-0005 git backstop until
  v3 ships a headless surface. Recommend folding into ADR-0006 before merge.

Commits left to claude-code (no Kiro git lane): commit `.kiro/**` + `.ai/**` as
`git -c user.name=kiro-cli` per the backstop territory rule.

## Why (owner directive 2026-07-09)
Owner adopted Kiro CLI v3 (`kiro-cli --v3`, now pinned in the 4AI-panes pane +
the headless dispatcher). Operating principle set today: **headless is the
default mode** — so Kiro MUST enforce mechanically in headless, which v2 does
NOT (validation campaign: `--trust-all-tools` makes v2 hooks + allowedPaths
inert; only prompt SAFETY RULES held). v3 replaces `--trust-all-tools` with a
declarative **`permissions.yaml`** capability model — that is the mechanism
that finally gives Kiro real headless enforcement. Design authority: ADR-0006
(being drafted by claude-code, cross-refs ADR-0002/0003/0004 Kiro portions).

## Ground truth first — READ the v3 docs
Read https://kiro.dev/docs/cli/v3/ AND its sub-pages (permissions, hooks,
agent config) for the EXACT v3 schemas. Do not assume from memory — v3 is
early-access and breaking. Confirm: `permissions.yaml` location + schema;
`.kiro/hooks/*.json` v3 hook schema (versioned, shell + agent-prompt actions,
trigger names); the unified **Markdown** agent config format (tag-based tools,
unified permissions block, inline MCP) that replaces `toolsSettings`.

## Constraints
- **ADDITIVE, non-destructive.** v3 runs ALONGSIDE v2. ADD the v3 config
  files; **KEEP** the v2 `.kiro/agents/*.json` + `.kiro/hooks/*.sh` guards as
  the v2 fallback (do NOT delete them this handoff). We migrate, we don't
  burn the bridge.
- **Preserve enforcement intent.** The v3 `permissions.yaml` must encode the
  same boundaries the v2 guards do: block writes to other CLIs' dirs
  (`.claude/`, `.kimi/`, `.opencode/`), sensitive files (`.env*`, keys,
  credentials), root non-allowlist (ADR-0001), and the removed-graph
  tombstones (`.kimigraph/`, `.kirograph/`). Kiro's own writable lane =
  `.kiro/` + `.ai/` + project source per its executor role.
- **No python dependency** in any new hook script (same WindowsApps-stub
  fail-open class we just fixed in the v2 guards — don't reintroduce it).

## Steps
1. Create `permissions.yaml` (v3) encoding the boundaries above as capability
   policies. This is the PRIMARY deliverable — the headless enforcement layer.
2. Migrate hooks to `.kiro/hooks/*.json` (v3 schema) as defense-in-depth
   (keep the v2 `.sh` guards in place as fallback).
3. Migrate the agent config to the v3 markdown format (unified permissions
   block, tag-based tools) — at minimum the `orchestrator` agent the
   dispatcher pins via `--agent orchestrator`. Keep v2 `.json` agents too.
4. **RE-VALIDATE under v3 (the whole point — live evidence, not unit tests):**
   run `kiro-cli --v3 chat --no-interactive --agent orchestrator` (drop
   `--trust-all-tools` if v3 rejects it; use the v3 permissions model) and
   attempt: (a) a main-thread write to `.claude/probe.txt` → must be BLOCKED
   by permissions.yaml (this is the T-K2 gap — prove it's closed HEADLESS);
   (b) a destructive `rm -rf /` → blocked; (c) a legit `.kiro/` or `.ai/`
   write → allowed. Paste the verbatim block output + `git status` proving
   nothing landed. If v3 STILL can't enforce headless, report that plainly —
   it changes the merge decision.
5. If v3 changed the headless flag surface (e.g. `--trust-all-tools` removed /
   `--no-interactive` renamed), report the correct v3 headless invocation so
   claude-code can update `dispatch-handoffs.sh` + the Selector pane.

## Report back with
- The v3 files created (paths) + `permissions.yaml` content summary.
- **Live v3 enforcement evidence** for (a)/(b)/(c) — verbatim.
- Corrected v3 headless invocation if flags changed.
- Set Status: DONE + move to `to-kiro/done/`; prepend activity entry
  (identity `kiro-cli`). Leave commits to claude-code (no Kiro git lane) —
  I'll commit `.kiro/**` as `git -c user.name=kiro-cli` (backstop territory).
- Write report to `.ai/reports/kiro-cli-2026-07-09-v3-migration.md`.
