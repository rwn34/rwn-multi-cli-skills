# Pane layout: interactive strip 50% -> 65%, auto-panes 35%
Status: DONE — default 0.65 (env RWN_4AI_TOP_FRACTION, clamp [0.2,0.8]),
  Selector.ps1 + test-selector-e2e.ps1 (Test 4 pinned, new Test 7),
  CHANGELOG Unreleased bullet, PR #73 opened, review routed to Kimi
  (.ai/handoffs/to-kimi/open/202607130215-review-pr73-top-strip-fraction.md).
  Did NOT merge (Kimi reviews first, Tier B fleet-merge per ADR-0011).
Sender: claude-code
Recipient: kiro-cli
Created: 2026-07-12 22:15
Auto: yes
Risk: A
Base: origin/master

## Goal (owner request, 2026-07-12)
The 6-pane fleet tab currently splits the tab **50/50** between:
- **TOP strip** — the two INTERACTIVE cockpits (Claude left, Kimi right), where the
  owner actually works
- **BOTTOM strip** — the 4 auto pane-runners (claude / kiro / kimi / opencode)

The owner wants the interactive strip **enlarged to 65%**, leaving **35%** for the
4 auto panes:

> "50%:50% vertical can be enlarged for kimi and claude as the interactive place to
> 65:35 for now; 35% for the ai panes we have (4 cli)"

## Current state
`tools/4ai-panes/Selector.ps1` line 47:
```powershell
$topStripFraction = 0.50
```
It is **hardcoded** — no env override. It is consumed at line ~624:
```powershell
$bottomFraction = [math]::Round(1 - $topStripFraction, 4)
$stages.Add("split-pane -H -s $bottomFraction -d $dq$TargetDir$dq $runner0")
```
So `0.50` -> bottom strip gets `0.50`.

## Target
1. **Default becomes `0.65`** (top strip = 65%, auto-panes = 35%).
2. **Make it env-tunable** — the owner said "for now", so they will likely retune it.
   Follow the existing convention in this same file (`RWN_4AI_PANE_DELAY_MS`,
   `RWN_4AI_TAB_DELAY_MS` at lines 54-55, 71-72, read via a `Get-DelayMs`-style
   helper that parses defensively). Add e.g. **`RWN_4AI_TOP_FRACTION`**.
   - Parse defensively: non-numeric, empty, or out-of-range -> fall back to the
     default `0.65`, never crash.
   - **Clamp to a sane range** (e.g. 0.2–0.8). A value of `0.99` would leave the four
     auto panes a sliver tall; `0.01` would bury the cockpits. Pick the bounds, state
     them, and say why. Out-of-range should clamp-with-warning or fall back to
     default — your call, justify it.
   - Document it in the same env-var comment block as the delay knobs (lines ~54-55).
3. Update the comment at line ~1404 which says `topStripFraction = 50% tall` — it will
   be a lie the moment you change the default.

## Watch out
- `tools/4ai-panes/test-selector-e2e.ps1` (81 assertions) asserts on the constructed
  `wt` stage strings, which **contain the fraction** (`split-pane -H -s 0.5 ...`).
  Changing the default WILL break those assertions. **Update them deliberately** — do
  not weaken an assertion to make it pass; change the expected value and say you did.
  Add a case pinning the new default and a case for the env override.
- The `5pane` layout mode also uses `$topStripFraction` (single full-width top cockpit
  over the runners). Confirm the new default is sane there too, or scope the change if
  it isn't. State what you found.
- Do NOT touch `tools/4ai-panes/pane-runner.ps1` — Kimi is building a fleet supervisor
  on it (handoff `202607122130`). Your change is `Selector.ps1` only.

## Constraints
- **Version:** ADR-0012 is live — the version is assigned **at merge**, not on the
  branch. **Do NOT bump `package.json`.** Bullets under `## [Unreleased]`. Confirm by
  grepping `.github/workflows/gates.yml` for `if: github.event_name == 'push'`.
- PowerShell 5.1; a pre-commit hook parse-gates every staged `.ps1`.

## Tests
- Default (no env) -> top strip 0.65 / bottom 0.35; assert the exact `split-pane -H -s 0.35`
  stage string.
- `RWN_4AI_TOP_FRACTION=0.5` -> restores the old 50/50 (proves the override works).
- Garbage / empty / out-of-range values -> default or clamp per your design; assert it.
- All 81 existing selector assertions still pass (with the fraction expectations
  deliberately updated).

## Verify (execute, paste)
- Full `test-selector-e2e.ps1` output.
- The constructed stage strings for the default and for one override, so the owner can
  see the actual `wt` commands.
- Parse-check on `Selector.ps1`.

## Note on deployment
The post-commit hook syncs `tools/4ai-panes/*` to the owner's live launcher at
`C:\Users\rwn34\.rwn-auto\rwn-4AI-panes\`. **The owner will see this change the next
time they launch the fleet**, so get it right — a bad fraction is immediately visible
and annoying. (A separate handoff, `202607122200`, is adding a provenance check to that
sync hook; unrelated to your change but be aware the sync exists.)

## Deliverable
Branch `exec/kiro/top-strip-fraction` off `origin/master`. Push, open a PR, route peer
review to **KIMI**. Do NOT merge (Kimi reviews; then the fleet merges — Tier B).

## Report back with
- the new default + the env var name + the clamp range and why
- the constructed stage strings (default + override)
- full test output
- confirmation the 5pane mode is still sane
- PR URL

## When complete (protocol v3)
Self-retire: set Status `DONE`, move to `.ai/handoffs/to-kiro/done/`.
