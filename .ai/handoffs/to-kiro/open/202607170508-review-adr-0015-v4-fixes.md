# Review ADR-0015 protocol v4 fixes

Status: OPEN
Sender: kimi-auto
Recipient: kiro
Owner: kiro
Created: 2026-07-17 05:08 (UTC+7)
Auto: yes
Risk: B
Base: origin/main
Observed-in: main@536d0a72bf1b08a17ac4dfd69a0fcf5389c824a0
Evidence: VERIFIED

## Goal

Review the implementation branch for ADR-0015's required corrections to the
shipped protocol v4 code, then open a PR to main. Do NOT merge — Claude holds the
merge gate.

Branch: `exec/kimi/202607171103-adr-0015-v4-fixes`
Commits: `6293c3f` (implementation), `769c1c9` (log/report), `d1605b6` (restore append-only log + prepend)

What to verify:
1. `.ai/tools/dispatch-handoffs.sh`
   - `Observed-in`: resolves sender SHA via `git rev-parse --verify`, accepts
     ancestors via `merge-base --is-ancestor`, fails only on divergence or
     unknown commit.
   - `Evidence: HYPOTHESIS`: dispatches at Risk A/B (verify-first); Risk C with
     HYPOTHESIS is rejected by lint, not dispatched.
   - `Gate-satisfied-by\/: hard gates (production deploy, publish to a public
     registry, tag/release cut, force-push, destructive ops on shared history,
     git reset --hard on shared state, secrets, production data) always HOLD
     regardless of Gate-satisfied-by; non-hard-gate Risk C with Gate + satisfied
     gate may dispatch; missing Gate HOLDs.
2. `.ai/tools/lint-handoff.sh` — rejects HYPOTHESIS + Risk C; keeps DONE-evidence
   and HYPOTHESIS-priority checks.
3. `.ai/tests/test-dispatch-worktree.sh` — 77/0 passed, including rewritten v4-3
   (hard-gate HOLD), v4-3b (non-hard-gate DISPATCH), v4-4b (abbreviated SHA),
   v4-4c (ancestor SHA), v4-5 (HYPOTHESIS + Risk C lint error).
4. `docs/specs/handoff-protocol-v4.md` — routing matrix and field semantics
   match ADR-0015.
5. `bash .ai/tools/check-ssot-drift.sh` and `bash .ai/tools/sync-replicas.sh --check`
   both report Drift: 0.

## Evidence

- Execution report: .ai/reports/kimi-202607170335-adr-0015-v4-fixes-report.md
- ADR: docs/architecture/0015-handoff-protocol-v4.md (branch exec/claude/202607170308-ratify-adr-0015-handoff-protocol-v4)
- Original execution handoff: .ai/handoffs/to-kimi/done/202607170335-implement-adr-0015-v4-fixes.md

## Report back with

- PR number and URL
- Review verdict (approved / changes requested / BLOCKED)
- Any blockers or concerns

## Blocker

—
