# P2 — Sanctioned scratch dir for one-off scripts (backlog #2/#33)
Status: DONE
Sender: claude-code
Recipient: kimi-cli
Created: 2026-07-09 22:01
Auto: yes
Risk: B

## Why
Owner pain: no home for throwaway/one-off scripts without polluting `scripts/`
or fighting the root-file guard. Fix = a gitignored `.scratch/` dot-dir
(root-rule exempt by nature).

## Task
1. Add to root `.gitignore`: an entry `/.scratch/` (with a short comment
   "# ephemeral one-off scripts + probe artifacts — never committed").
2. Create `.scratch/README.md` explaining: this dir is gitignored, is the
   sanctioned home for one-off/throwaway scripts and probe files, never commit
   its contents. (The README itself: since the dir is gitignored, the README
   won't be tracked either — that's fine; OR add a `!/.scratch/README.md`
   negation to `.gitignore` so ONLY the README is tracked as documentation.
   Your call — pick the cleaner option and note which.)
3. Add a dated note to `docs/architecture/0001-root-file-exceptions.md` (a small
   appended NOTE, don't rewrite): `.scratch/` is the sanctioned ephemeral
   scratch location; one-off scripts go there, not at root or in `scripts/`.

## Rules
- Write only root `.gitignore`, `.scratch/`, and the ADR note. Do NOT touch
  other CLIs' dirs.
- Prepend your activity entry with `bash .ai/tools/activity-append.sh` (atomic).
  Set Status: DONE + move to `.ai/handoffs/to-kimi/done/`. Commit + push your
  work if you have git access; else leave for claude-code.

## Report
Activity entry: the `.gitignore` lines, the ADR note, and which README option
you chose.
