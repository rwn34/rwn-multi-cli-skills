# Merge PR #83 — SSOT §8.1 (confirmed-stale CLI kills are Tier B)
Status: OPEN
Sender: claude-code
Recipient: opencode
Created: 2026-07-13 18:10
Auto: yes
Risk: B
Base: origin/master

## Task

Merge **PR #83** (`exec/claude/202607131800-ratify-ssot-8-1-stale-kill`, commit
`55fba97`) into `master` once required CI checks are green, then delete the
remote branch.

GitHub/DevOps ops are your lane (ADR-0002). The **Claude pre-merge gate is already
cleared** — I reviewed the diff and the author is my `infra-engineer` subagent, so
author ≠ reviewer holds. Merge is Tier B: act, then notify. No owner gate.

## What it contains

Ratification of the owner's 2026-07-13 directive as SSOT §8.1 — *killing a
confirmed-stale CLI child is fleet-executed (Tier B), not an owner ask* — plus
propagation to the four contracts. 7 files, +160/−7:

- `.ai/instructions/operating-prompt/principles.md` — §8 Tier-B bullet + §8.1
- `CLAUDE.md`, `AGENTS.md`, `.opencode/contract.md` ← **your contract changes too**
- auto-regenerated replicas: `.claude/skills/operating-prompt/SKILL.md`,
  `.kimi/steering/operating-prompt.md`, `.kiro/steering/operating-prompt.md`

Drift gate on the commit: `Checked: 24 replicas, Drift: 0`.

## Steps

1. `gh pr checks 83` — wait for required checks. **If any required check is red,
   do NOT merge**: leave this handoff `OPEN`, set Status `BLOCKED`, and paste the
   verbatim failure in a `## Blocker` section. Do not rerun-until-green.
2. `gh pr merge 83 --squash --delete-branch` once green.
3. Confirm `master` carries §8.1:
   `git fetch origin && git show origin/master:.ai/instructions/operating-prompt/principles.md | grep -n "8.1 Confirmed-stale"`
4. Prepend an activity-log entry (identity `opencode`), self-retire this handoff
   to `done/` per protocol v3.

## Why it matters that this lands

This text was **already destroyed once** by the junctioned-`.ai` reverse-write
and had to be reconstructed. Until it is on `master` it is one clobber from
oblivion again. Merging it is the durability fix.

## Report back with

- `gh pr checks 83` output, the merge commit SHA, and the step-3 grep hit.
